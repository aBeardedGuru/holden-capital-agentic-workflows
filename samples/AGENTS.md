# AGENTS.md - samples Node Access Rules

This node holds sample payloads and example text fixtures used to reason about contracts and test workflows.

## Rules

- Samples should be realistic but scrubbed.
- Never include real credentials, full account numbers, or sensitive PII.
- Keep sample payloads aligned with current schema versions.
- If a schema changes, update or replace affected samples.
